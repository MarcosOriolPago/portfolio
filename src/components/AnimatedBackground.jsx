import React from 'react';
import ParticlesBg from 'particles-bg';

const Background = () => {
  return (
    <ParticlesBg color="#ff0000" num={100} type="line" bg={true} />
  );
};

export default Background;
